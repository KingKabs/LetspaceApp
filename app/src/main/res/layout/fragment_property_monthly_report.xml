<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fragmentPropertyReport"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/dashboard_bg"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:id="@+id/tvPropertyReportTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monthly Reports"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/dashboard_title"
        android:layout_marginBottom="12dp"/>

    <!-- RecyclerView for monthly reports -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMonthlyReports"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="8dp"/>
</LinearLayout>
